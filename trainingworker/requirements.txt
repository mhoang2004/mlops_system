# Shared deps — used by BOTH CPU and GPU builds.
# torch / torchvision are installed separately in each Dockerfile.

# Task queue
celery[redis]>=5.3.6
redis>=5.0.1

# Object storage
minio>=7.2.0

# API callbacks
requests>=2.31.0

# Validation
pydantic>=2.5.0

# Numerics / image
numpy>=1.26.0
Pillow>=10.0.0

# Experiment tracking
mlflow>=2.14.0

# Object detection
ultralytics>=8.2.0

# Segmentation (Mamba-UNet)
albumentations>=1.3.0
einops>=0.7.0
mamba-ssm>=1.2.0; sys_platform == "linux"  # CUDA-only, skip on non-Linux/CPU
